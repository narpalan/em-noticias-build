import styled from 'styled-components';
import { Paper } from '@mui/material';

const FooterStyled = styled(Paper)`  
  position: fixed;
  bottom: 0;
  left: 0;
  right: 0;
`;

export default FooterStyled;
